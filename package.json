{
  "name": "dalalah-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "دِلالة | DALALAH — Arabic (RTL) real estate & vehicle marketplace prototype for Saudi Arabia.",
  "scripts": {
    "dev": "serve public -l 3000",
    "start": "serve public -l 3000",
    "build": "echo \"Static site — no build step required. Deployable output is the /public folder.\""
  },
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=16"
  }
}
